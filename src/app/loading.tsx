import { DashboardSkeleton } from "@/components/shared/LoadingSkeleton"

export default function Loading() {
    // Use the premium dashboard skeleton as the global loading state
    return <DashboardSkeleton />
}
